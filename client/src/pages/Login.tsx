/** FlowGuard Calm Operations Console: a composed, theme-aware sign-in surface that introduces product value before operational detail. */
import { useState } from "react";
import { ArrowRight, CheckCircle2, Eye, EyeOff, Moon, ShieldCheck, Sun } from "lucide-react";
import { useLocation } from "wouter";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { useAuth } from "@/contexts/AuthContext";
import { useTheme } from "@/contexts/ThemeContext";
import { flowguardApi } from "@/lib/flowguard-api";
import { toast } from "sonner";

export default function Login() {
  const { signIn } = useAuth();
  const { theme, toggleTheme } = useTheme();
  const [, navigate] = useLocation();
  const [email, setEmail] = useState("admin@flowguard.demo");
  const [password, setPassword] = useState("demo123");
  const [showPassword, setShowPassword] = useState(false);
  const [loading, setLoading] = useState(false);

  const submit = async (event: React.FormEvent) => {
    event.preventDefault();
    setLoading(true);
    try {
      const result = await flowguardApi.login(email, password);
      signIn(result);
      navigate("/");
      toast.success("Control room is ready.");
    } catch (error) {
      toast.error(error instanceof Error ? error.message : "Unable to sign in. Make sure the local API is running.");
    } finally {
      setLoading(false);
    }
  };

  return <main className="login-surface min-h-svh bg-slate-950 p-4 lg:p-7"><div className="mx-auto grid min-h-[calc(100vh-2rem)] max-w-[1440px] overflow-hidden rounded-3xl bg-[#F7F8FA] shadow-2xl dark:bg-slate-900 dark:shadow-black/35 lg:grid-cols-[1.08fr_0.92fr]">
    <section className="login-grid relative hidden overflow-hidden bg-slate-900 p-10 text-white lg:flex lg:flex-col"><img src="https://images.unsplash.com/photo-1558494949-ef010cbdcc31?auto=format&fit=crop&w=1600&q=85" alt="Abstract technology infrastructure" className="absolute inset-0 size-full object-cover opacity-65 mix-blend-screen" /><div className="absolute inset-0 bg-[linear-gradient(110deg,rgba(15,23,42,0.97)_0%,rgba(15,23,42,0.82)_47%,rgba(15,23,42,0.32)_100%)]" /><div className="relative z-10 flex items-center gap-3"><img src="https://files.manuscdn.com/user_upload_by_module/session_file/310519663889343004/TJucKfGMGLCqWyAe.png" alt="FlowGuard mark" className="size-10 rounded-xl" /><span className="text-lg font-extrabold tracking-[-0.04em]">FlowGuard</span></div><div className="relative z-10 my-auto max-w-xl"><p className="mb-4 text-[11px] font-bold tracking-[0.18em] text-blue-300">DURABLE WORKFLOW OPERATIONS</p><h1 className="text-5xl font-extrabold leading-[0.98] tracking-[-0.065em]">Know what happened. Decide what happens next.</h1><p className="mt-6 max-w-md text-base leading-7 text-slate-300">Coordinate orders, inventory, payments, approvals, and recovery with a complete record of every decision.</p><div className="mt-8 max-w-md rounded-xl border border-white/15 bg-slate-950/45 p-4 backdrop-blur-sm"><div className="flex items-center justify-between"><span className="font-mono text-[10px] text-slate-300">ORDER-1042 · CORR-7F2B</span><span className="inline-flex items-center gap-1.5 rounded-md bg-amber-300/15 px-2 py-1 text-[10px] font-bold tracking-[0.08em] text-amber-200"><span className="size-1.5 rounded-full bg-amber-300" />DECISION DUE</span></div><div className="mt-4 flex items-center gap-3"><div className="flex items-center gap-2"><span className="flex size-5 items-center justify-center rounded-full bg-emerald-400 text-[10px] text-slate-950">✓</span><span className="text-[11px] font-semibold text-slate-100">Payment authorized</span></div><span className="h-px flex-1 bg-gradient-to-r from-emerald-300 via-blue-400 to-amber-300" /><div className="flex items-center gap-2"><span className="flex size-5 items-center justify-center rounded-full bg-amber-300 text-[10px] text-slate-950">!</span><span className="text-[11px] font-semibold text-slate-100">Owner: Asha</span></div></div><p className="mt-3 text-[11px] leading-5 text-slate-300">The next irreversible action is blocked until an authorized human decision is recorded.</p></div><div className="mt-8 grid grid-cols-3 gap-3">{[["01", "Persist state"], ["02", "Wait safely"], ["03", "Recover clearly"]].map(([number, label]) => <div key={number} className="border-l border-white/25 pl-3"><span className="font-mono text-xs text-blue-300">{number}</span><p className="mt-1 text-xs font-semibold text-white">{label}</p></div>)}</div></div><div className="relative z-10 flex items-center gap-2 text-xs text-slate-300"><ShieldCheck className="size-4 text-emerald-300" />Durable execution. Human judgment. Complete history.</div></section>
    <section className="relative flex items-center justify-center p-6 dark:bg-slate-900 sm:p-12"><Button onClick={toggleTheme} variant="outline" size="icon" className="fg-interactive absolute right-5 top-5 border-slate-200 bg-white text-slate-600 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-200" aria-label={theme === "dark" ? "Switch to light mode" : "Switch to dark mode"} title={theme === "dark" ? "Switch to light mode" : "Switch to dark mode"}>{theme === "dark" ? <Sun className="size-4" /> : <Moon className="size-4" />}</Button><div className="w-full max-w-sm"><div className="mb-10 flex items-center gap-3 lg:hidden"><img src="https://files.manuscdn.com/user_upload_by_module/session_file/310519663889343004/TJucKfGMGLCqWyAe.png" alt="FlowGuard mark" className="size-10 rounded-xl" /><span className="text-xl font-extrabold tracking-[-0.04em] text-slate-900 dark:text-slate-100">FlowGuard</span></div><p className="text-[10px] font-bold tracking-[0.16em] text-blue-600 dark:text-blue-300">SECURE ACCESS / ACTIVE PATH</p><h2 className="mt-3 text-3xl font-extrabold tracking-[-0.055em] text-slate-900 dark:text-slate-100">Enter the decision workspace</h2><p className="mt-3 text-sm leading-6 text-slate-500 dark:text-slate-400">Sign in to review evidence, make an authorized decision, or begin a safe recovery path.</p><form onSubmit={submit} className="mt-8 space-y-5"><label className="block"><span className="mb-2 block text-xs font-bold text-slate-700 dark:text-slate-200">Email address</span><Input value={email} onChange={event => setEmail(event.target.value)} type="email" className="h-11 border-slate-200 bg-white dark:border-slate-700 dark:bg-slate-800 dark:text-slate-100" /></label><label className="block"><span className="mb-2 block text-xs font-bold text-slate-700 dark:text-slate-200">Password</span><div className="relative"><Input value={password} onChange={event => setPassword(event.target.value)} type={showPassword ? "text" : "password"} className="h-11 border-slate-200 bg-white pr-11 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-100" /><button type="button" aria-label={showPassword ? "Hide password" : "Show password"} onClick={() => setShowPassword(value => !value)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-700 dark:hover:text-slate-200">{showPassword ? <EyeOff className="size-4" /> : <Eye className="size-4" />}</button></div></label><Button type="submit" disabled={loading} className="h-11 w-full bg-blue-600 font-bold hover:bg-blue-700">{loading ? "Verifying access…" : <>Review workflow evidence <ArrowRight className="ml-2 size-4" /></>}</Button></form><p className="mt-5 text-center text-xs text-slate-500 dark:text-slate-400">New to FlowGuard? <Link href="/register" className="font-bold text-blue-600 hover:text-blue-700 dark:text-blue-300">Create an account</Link></p><div className="mt-7 rounded-xl border border-slate-200 bg-white p-4 dark:border-slate-700 dark:bg-slate-800"><p className="mb-2 flex items-center gap-2 text-xs font-bold text-slate-800 dark:text-slate-100"><CheckCircle2 className="size-4 text-emerald-500" />Prepared demo access</p><p className="text-xs leading-5 text-slate-500 dark:text-slate-400"><span className="font-mono text-slate-700 dark:text-slate-200">admin@flowguard.demo</span> · Password: <span className="font-mono text-slate-700 dark:text-slate-200">demo123</span></p></div></div></section>
  </div></main>;
}
